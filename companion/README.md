# LeetDock Local Network

This companion runs LeetDock authentication and LeetCode network requests in
VS Code's local Extension Host. The main LeetDock extension uses it
automatically, including from Remote-SSH, WSL, and Dev Container windows.

Install and use the main `cooronx.leetdock` extension. This companion does not
contribute user-facing commands or views.
